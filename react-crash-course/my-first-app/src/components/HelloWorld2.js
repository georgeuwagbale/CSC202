import React from 'react';

const HelloWorld2 = (props) => {
    return(
        <div>
            <p>Hello {props.name}. Greetings form React using Js.</p>
        </div>
    );
}

export default HelloWorld2;